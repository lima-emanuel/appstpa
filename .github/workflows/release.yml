name: Release

on:
    push:
        tags:
            - v3.*
    workflow_dispatch:
        inputs:
            push_latest:
                description: "Also push as latest tag"
                required: true
                type: boolean
                default: false

jobs:
    build-linux:
        name: python
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v6

            - name: "Set up Python"
              uses: actions/setup-python@v6
              with:
                  python-version-file: ".python-version"

            - name: Install uv
              uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
              with:
                  version: "0.11.9"

            - name: Enable caching
              uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
              with:
                  enable-cache: true

            - name: Install the project
              run: uv sync --locked --all-extras --all-groups

            - name: Build the binary
              run: uv run pyinstaller --onefile --windowed --name "AppSTPA-Linux-x86" app.py

            - name: Upload the binary
              uses: actions/upload-artifact@v7
              with:
                  name: AppSTPA-Linux-x86
                  path: dist/AppSTPA-Linux-x86
                  if-no-files-found: error

    build-windows:
        name: python
        runs-on: windows-latest

        steps:
            - uses: actions/checkout@v6

            - name: "Set up Python"
              uses: actions/setup-python@v6
              with:
                  python-version-file: ".python-version"

            - name: Install uv
              uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
              with:
                  version: "0.11.9"

            - name: Enable caching
              uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
              with:
                  enable-cache: true

            - name: Install the project
              run: uv sync --locked --all-extras --all-groups

            - name: Build the binary
              run: uv run pyinstaller --onefile --windowed --name "AppSTPA-Windows-x86" app.py

            - name: Upload the binary
              uses: actions/upload-artifact@v7
              with:
                  name: AppSTPA-Windows-x86
                  path: dist/AppSTPA-Windows-x86
                  if-no-files-found: error

    build-macos:
        name: python
        runs-on: macos-latest

        steps:
            - uses: actions/checkout@v6

            - name: "Set up Python"
              uses: actions/setup-python@v6
              with:
                  python-version-file: ".python-version"

            - name: Install uv
              uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
              with:
                  version: "0.11.9"

            - name: Enable caching
              uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
              with:
                  enable-cache: true

            - name: Install the project
              run: uv sync --locked --all-extras --all-groups

            - name: Build the binary
              run: uv run pyinstaller --onefile --windowed --name "AppSTPA-MacOS-ARM" app.py

            - name: Upload the binary
              uses: actions/upload-artifact@v7
              with:
                  name: AppSTPA-MacOS-ARM
                  path: dist/AppSTPA-MacOS-ARM
                  if-no-files-found: error
